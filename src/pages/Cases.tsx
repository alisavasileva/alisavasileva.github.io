import { Component } from 'solid-js'
import { RouteSectionProps } from '@solidjs/router'
import { ProjectCards } from '@/components/ProjectCards'

export const CasesPage: Component<RouteSectionProps> = () => {
  return <ProjectCards />
}
